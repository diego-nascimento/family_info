import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  TimeButtonsWrapper: {
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
  TimeButtonContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  TimeButtonText: {
    fontSize: 16,
    marginLeft: 5,
  },
});
