import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  TitleContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: 10,
  },
  Title: {
    fontSize: 18,
    fontWeight: '600',
  },
});
